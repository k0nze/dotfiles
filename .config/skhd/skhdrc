# ignores ralt + q (passed through to karabiner to typ @ on a non-Apple keyboard)
ralt - q -> : 

# changing window focus within a space
#lalt - h : yabai -m window --focus west
#lalt - j : yabai -m window --focus south
#lalt - k : yabai -m window --focus north
#lalt - l : yabai -m window --focus east

# flip layout along x- or y-axis
lshift + lalt - y : yabai -m space --mirror y-axis
lshift + lalt - x : yabai -m space --mirror x-axis

# swap windows
lshift + lalt - h : yabai -m window --swap west
lshift + lalt - j : yabai -m window --swap south
lshift + lalt - k : yabai -m window --swap north
lshift + lalt - l : yabai -m window --swap east

# move focused window to next space
#shift + lcmd - left : yabai -m window --space next; yabai -m space --focus next
# move focused windows to previous space
#shift + lcmd - right : yabai -m window --space prev; yabai -m space --focus prev

# increase window size
lshift + lctrl - h : yabai -m window --resize left:-20:0
lshift + lctrl - j : yabai -m window --resize bottom:0:20
lshift + lctrl - k : yabai -m window --resize top:0:-20
lshift + lctrl - l : yabai -m window --resize right:20:0

# decrease window size
#lshift + lcmd - h : yabai -m window --resize right:-20:0
#lshift + lcmd - j : yabai -m window --resize top:0:20
#lshift + lcmd - k : yabai -m window --resize bottom:0:-20
#lshift + lcmd - l : yabai -m window --resize left:20:0

# TODO: https://jasoneckert.github.io/myblog/customizing-macos-for-development/skhdrc.txt
